# システム構成

> **ひとことで：** ThreatGuard はフロントエンド・API・データベースを分離したクラウドアーキテクチャで、安全かつスケーラブルに運用されています。

## 全体構成図

```
┌──────────────┐     ┌──────────────┐     ┌──────────────────────┐
│   ブラウザ    │────▶│     CDN      │────▶│   フロントエンド      │
│  (ユーザー)   │     │              │     │   (SSR/SSG)          │
└──────────────┘     └──────────────┘     └──────────────────────┘
                                                    │
                                                    ▼ API呼び出し
                                          ┌──────────────────────┐
                                          │   APIサーバー         │
                                          │                      │
                                          │  ├ 認証              │
                                          │  ├ スキャンエンジン    │
                                          │  ├ ブラウザエンジン   │
                                          │  ├ AI分析            │
                                          │  └ メール送信        │
                                          └──────────────────────┘
                                             │          │
                                    ┌────────┘          └────────┐
                                    ▼                            ▼
                          ┌──────────────┐            ┌──────────────┐
                          │     RDB      │            │   キャッシュ  │
                          │              │            │              │
                          │  脅威データ   │            │  セッション   │
                          │  ユーザー    │            │              │
                          │  テナント    │            │              │
                          └──────────────┘            └──────────────┘
```

## コンポーネント詳細

### フロントエンド

| 項目 | 内容 |
|------|------|
| フレームワーク | Next.js 14（React 18）/ App Router |
| 言語 | TypeScript |
| スタイリング | Tailwind CSS |
| チャート | Recharts |
| ホスティング | Vercel（CDN配信） |
| 認証 | JWT トークンベース認証 |

### APIサーバー

| 項目 | 内容 |
|------|------|
| フレームワーク | Express.js（TypeScript） |
| ORM | Prisma |
| ジョブキュー | BullMQ（Redis） |
| スケジューラ | node-cron（定期スキャン） |
| ブラウザエンジン | Playwright（Chromium） |
| AI分析 | Anthropic Claude API |
| 画像処理 | sharp |
| バリデーション | Zod |
| ホスティング | Railway（Docker コンテナ） |
| ファイルストレージ | 永続ボリューム（スクリーンショット・アップロード） |

### データベース

| 項目 | 内容 |
|------|------|
| 種別 | PostgreSQL（本番環境） |
| ORM | Prisma（マイグレーション管理） |
| ホスティング | マネージドクラウド |

### キャッシュ / キュー

| 項目 | 内容 |
|------|------|
| 種別 | Redis |
| 用途 | BullMQ ジョブキュー（非同期スキャン処理） |

### 外部サービス連携

| サービス | 用途 |
|---------|------|
| Anthropic Claude API | 脅威分類・テイクダウン文面生成 |
| crt.sh / CertSpotter | CT（Certificate Transparency）ログの検索 |
| RDAP | ドメイン登録情報照会 |
| Resend | メール送信API（テイクダウン・通知） |
| SMTP | ブランド別メール送信（nodemailer） |
| Slack Webhook | アラート通知（組織ごとに個別設定可能） |
| Twitter/X API | SNS上のブランドなりすまし監視 |
| Google Safe Browsing | ブラウザへのフィッシング報告 |
| Microsoft SmartScreen | Edge ブラウザへのフィッシング報告 |

## マルチテナント設計

ThreatGuard はマルチテナントアーキテクチャを採用しています。

```
Organization（組織）
  └── Brand（ブランド）- 複数登録可
        └── DetectedDomain（検出ドメイン）
              ├── WebProbe（調査結果）- 履歴保持
              ├── ThreatAnalysis（AI分析）- 履歴保持
              └── TakedownRequest（削除申請）
```

- 各組織のデータは完全に分離
- API層で組織IDによるフィルタリングを実施
- システム管理者は全組織のデータにアクセス可能

## 認証・認可

| ロール | 権限 |
|--------|------|
| **システム管理者** | 全組織・全データへのアクセス。組織管理 |
| **組織管理者** | 自組織内の全操作。ブランド管理・ユーザー管理 |
| **一般ユーザー** | 自組織内の閲覧・基本操作 |

- トークンベース認証
- 各APIエンドポイントでロールチェック

## 次のステップ

- [データの取り扱い](/security/data-handling) — 収集データの保存と管理
- [準拠規格・法令](/security/compliance) — セキュリティポリシーと法令対応
